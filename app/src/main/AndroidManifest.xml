<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.macbitsgoa.pawpals">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_paws"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_paws_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FeedHistoryActivity" />
        <activity android:name=".AddFeedActivity" />
        <activity
            android:name=".AboutMAC"
            android:label="@string/title_activity_about_mac"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EmergencyContactsActivity"
            android:label="@string/title_activity_emergency_contacts"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>